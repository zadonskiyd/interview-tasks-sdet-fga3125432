plugins {
    // Позволяет Gradle автоматически скачать нужный JDK (toolchain 17),
    // если он не установлен в системе.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "interview-tasks-sdet-fga3125432"
