package top.lava.sdet.katas;

/**
 * Задача A («Строки»).
 *
 * <p>Реализуйте {@link #isPalindrome(String)} — проверку, является ли строка
 * палиндромом, <b>без учёта регистра</b> и <b>игнорируя</b> все символы, кроме
 * букв и цифр.
 *
 * <h2>Правила</h2>
 * <ul>
 *   <li>{@code "A man, a plan, a canal: Panama"} → {@code true};</li>
 *   <li>{@code "Argentina"} → {@code false};</li>
 *   <li>пустая строка или строка из одних разделителей (например, {@code ", . !"})
 *       → {@code true};</li>
 *   <li>{@code null} → {@link IllegalArgumentException}.</li>
 * </ul>
 */
public class Palindromes {

    public boolean isPalindrome(String input) {
        // TODO: реализуйте проверку согласно javadoc.
        throw new UnsupportedOperationException("Не реализовано — это часть задания.");
    }
}
